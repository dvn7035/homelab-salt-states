# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf

daemon=600
ssl=yes
use=web
protocol=googledomains
login="{{ pillar['ddclient']['login'] }}"
password="{{ pillar['ddclient']['password'] }}"
@.dvn7035.com
